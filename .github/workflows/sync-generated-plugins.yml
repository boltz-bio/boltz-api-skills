name: Sync generated plugin copies

on:
  push:
    branches-ignore:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    name: Regenerate generated plugin copies
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'github-actions[bot]' }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Generate distribution surfaces
        run: scripts/generate-surfaces.sh
      - name: Commit regenerated plugin copy
        run: |
          set -euo pipefail
          if [[ -z "$(git status --porcelain -- plugins/boltz plugins/boltz-compute-cli)" ]]; then
            echo "Generated plugin copies are already in sync."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugins/boltz plugins/boltz-compute-cli
          git commit -m "chore(plugins): sync generated plugin copies"
          git push origin "HEAD:${GITHUB_REF_NAME}"
